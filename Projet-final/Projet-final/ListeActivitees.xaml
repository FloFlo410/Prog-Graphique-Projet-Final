<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Projet_final.ListeActivitees"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Projet_final"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock>Nos Activités!</TextBlock>
        <ListView x:Name="lv_Activite">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Activite">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Nom}"/>
                        <TextBlock Text="{x:Bind Type}"/>
                        <TextBlock Text="{x:Bind PrixVente}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
