<Page
    x:Class="Projet_final.AjoutActivite"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Projet_final"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <Button FontSize="20" VerticalAlignment="Center" Grid.Row="0" Click="Button_Click">Retour</Button>
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" FontSize="60" HorizontalAlignment="Center">Ajout d'une activité</TextBlock>

        <TextBox x:Name="tbox_nom" Header="Nom de l'activité" Grid.Row="1" Grid.Column="0" Margin="0 20 30 0" FontSize="20"></TextBox>

        <StackPanel Grid.Row="1" Grid.Column="1" Background="Aqua" Orientation="Horizontal">
            <ComboBox x:Name="cbox_categories" Header="Catégorie de l'activité" Margin="0 20 30 0" FontSize="20" HorizontalAlignment="Stretch" ></ComboBox>
            <TextBox Header="Catégorie de l'activité" Margin="0 20 30 0" FontSize="20" HorizontalAlignment="Stretch" x:Name="tbox_categories" Visibility="Collapsed"  />
            <ToggleSwitch x:Name="switch_choix"  OffContent="Existe" OnContent="Nouveau" VerticalAlignment="Bottom" HorizontalAlignment="Right" Toggled="ToggleSwitch_Toggled" />
        </StackPanel>

        <NumberBox x:Name="num_prix_org" Header="Prix pour l'organisation ($)" Margin="0 20 30 0" Grid.Row="2" Grid.Column="0" FontSize="20" SpinButtonPlacementMode="Compact" SmallChange="10"/>

        <NumberBox x:Name="num_prix_client" Header="Prix pour le client ($)" Margin="0 20 30 0" Grid.Row="2" Grid.Column="1" FontSize="20" SpinButtonPlacementMode="Compact" SmallChange="10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0 20 30 0">
            <Button Margin="10" FontSize="20" x:Name="btn_modif" Click="btn_modif_Click" >Ajouter</Button>
        </StackPanel>



    </Grid>
</Page>
